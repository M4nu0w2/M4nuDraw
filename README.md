# Skribbl.io Clone Monorepo

## Setup
1. Run `npm install` in the root directory.
2. Setup Postgres database and update `apps/server/.env` with your Prisma connection string.
3. Run `npm run dev` to start both the frontend and backend in parallel.
